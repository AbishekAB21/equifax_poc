import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const background = Color(0xFF0D0F0E);
  static const surface = Color(0xFF171A18);
  static const card = Color(0xFF202421);

  // Accent
  static const primary = Color(0xFFA8E86C);
  static const primaryDark = Color(0xFF7FCB4F);

  // Text
  static const textPrimary = Color(0xFFF2F4F0);
  static const textSecondary = Color(0xFF9DA39D);

  // Borders / dividers
  static const border = Color(0xFF303632);

  // Other
  static const success = Color(0xFFA8E86C);
}